# Memory Index

- [vfkit REST shutdown & 5s force-timer](project_vfkit-rest-shutdown-and-5s-force-timer.md) — hardcoded 5s force-kill on any terminating signal; the REST+`set -m` workaround is verified wrong — the guest must power itself off
- [Backgrounded proc in $() hangs](project_backgrounded-proc-in-command-substitution-hangs.md) — `PID="$(start_listener)"` hangs unless the bg process's stdout is redirected; it holds the inherited stdout open
- [Docs locality: packaging vs. hook-event](project_docs-locality-packaging-vs-hook-event.md) — plugin-authoring-constraints.md (packaging) vs. hook-event-notes.md (per-event runtime facts); don't conflate
- [Commit heredoc gate](feedback_commit-heredoc-gate.md) — `git commit -m "$(cat <<EOF...)"` is blocked; use `git commit -F <scratch-file>` for multi-line messages
- [Worktree path, not main clone](feedback_worktree-path-not-main-clone.md) — Read/Edit the worktree-absolute path; the main-clone path is a different checkout and gives a stale file view
- [Cross-plugin lib sharing](project_cross-plugin-lib-sharing-resolved-for-real-config-needs.md) — a bare cross-plugin Read of skills/lib/repo-config.md never resolves; inline 1-2 real fields, or duplicate the full contract
- [Backtick comments in unquoted heredocs](project_backtick-comments-in-unquoted-heredocs.md) — paired backticks in a heredoc comment ARE command substitution; can masquerade as several unrelated bugs
- [Real-build verification, not unit tests](project_real-build-verification-not-unit-tests.md) — stub only the blocking external cmd, run the real script's real path, inspect the generated artifacts literally
- [Real boot that exercises mounts, from a worktree](project_real-boot-that-exercises-mounts-from-a-worktree.md) — host-acceptance never writes a mounts.tsv; boot with NO net (a worktree path breaks gvproxy's socket) and bump LAUNCHER_LOGIC_REV first
- [PR branch rebased under checkout](project_pr-branch-rebased-under-checkout.md) — the rebase-sweep automation can force-rebase a PR branch mid-session; recover via format-patch+am onto the new tip
- [Another worktree holds the branch; cwd resets](project_stale-worktree-holds-branch-and-cwd-does-not-persist.md) — don't inspect or remove it; work detached from `origin/<branch>` and push `HEAD:refs/heads/<branch>`
- [Git command-form gate: cd then bare git](feedback_git-command-form-gate-cd-then-bare-git.md) — both `cd X && git ...` and `git -C X ...` are blocked; only a bare `cd X` then a separate bare `git ...` call works
- [mkosi Minimize=guess sizes fs, not partition](project_mkosi-minimize-guess-sizes-fs-not-partition.md) — Minimize=guess tight-fits the ext4 FS while SizeMinBytes only pads the GPT slot; drop Minimize
- [mkosi v26 source verification technique](project_mkosi-v26-source-verification-technique.md) — use the `gh api` contents route (not WebFetch, not curl) to read mkosi's real Python source for its defaults
- [apt-get clean verified behavior](project_apt-get-clean-verified-behavior.md) — real-container-verified: removes archives + pkgcache.bin/srcpkgcache.bin, never touches /var/lib/apt/lists
- [claude-vm mkosi installs from outside image](project_claude-vm-mkosi-installs-from-outside-image.md) — a boot-time apt-get needs `apt` explicitly baked into the base Packages= list; mkosi's build-time apt never leaks in
- [claude-vm inspect raw image with debugfs](project_claude-vm-inspect-raw-image-with-debugfs.md) — dd-carve the ext4 partition then debugfs (it has no -o offset) to inspect a built guest.raw without loop devices
- [apt keyring extension vs content](project_apt-keyring-extension-vs-content.md) — apt >= 2.x picks armored-vs-binary by FILE EXTENSION not content, so render_apt_source(_boot) must sniff+rename
- [Repo-boundary gate blocks any out-of-repo tool arg](project_repo-boundary-gate-blocks-any-tool-arg-outside-repo.md) — find/cp/awk args resolving outside the repo are blocked even as a cp source, and so is `sed -i ''`'s empty operand; set TMPDIR inside the repo first
- [No invented policy in agent defs](feedback_no-invented-policy-in-agent-defs.md) — never assert a rule in an agent .md without grepping an actual rules file; your own prior addition can be the next defect
- [Sweep sibling agent guards](project_sweep-sibling-agent-guards.md) — sweep the exceptions and the inline operational text, not just the headline rule; collapse shared rationale to a pointer
- [Scope guarantee claims to actual caller](feedback_scope-guarantee-claims-to-actual-caller.md) — "autonomous mode always runs fresh" held only of one caller's habits, not the skill's own logic; name the caller
- [Bounded poll + unbounded wait is unbounded](project_bounded-poll-then-unbounded-wait.md) — the give-up branch must return BEFORE any blocking wait; assert it structurally with a negative control
- [Test harness holds the pipe open](project_test-harness-holds-pipe-open.md) — a long-lived stub in `$(...)` makes a timing test measure the pipe, not the code; return results via a file
- [Staleness check: both ends, one source](project_staleness-check-both-ends-same-source.md) — a "did it move?" check must record AND re-read from the same authoritative surface (gh headRefOid, not a cached ref)
- [Count tallies rot after the list too](project_count-tally-class-includes-back-references.md) — a §7 sweep also covers post-list back-references, YAML `description:` frontmatter and non-`.md` comments
- [Getty respawn is Restart=, not the leading `-`](project_getty-respawn-is-restart-not-dash.md) — stock serial-getty ships Restart=always; the image identity hash misses launcher source, so LAUNCHER_LOGIC_REV is the only invalidator
- [claude marketplace probe recipe](project_claude-marketplace-probe-recipe.md) — the CLI rejects file:// and treats a local path as Directory (no git); `marketplace update` exits 0 doing nothing
- [Worktree gate blocks compound bash](project_worktree-isolation-gate-blocks-compound-bash.md) — `&&`-chaining, a `$VAR` path or a `"$PWD"` argument is refused as unverifiable even when in-bounds; one bare command per call
- [Prove a config extends-chain is live](project_prove-config-inheritance-chain-live.md) — cleared violations never prove `extends` resolved; flip a value in the PARENT, and break the path to confirm it fails closed
- [Lint-config control needs its own directory](project_lint-config-control-needs-its-own-directory.md) — markdownlint-cli2's `--config` loses to directory discovery, so a `--config` baseline silently measures the NEW config
- [PR body is a swept surface](feedback_pr-body-is-a-swept-surface.md) — a fix round that changes a mechanism the PR body names updates its What changed / Decisions bullets that same round
- [git status cannot see main staleness](project_git-status-cannot-see-main-staleness.md) — only `git merge-base --is-ancestor origin/main HEAD` says whether you are behind, and a CONFLICTING PR never self-heals
- [rebase --continue editor gate](feedback_rebase-continue-editor-gate.md) — `GIT_EDITOR=true git ...` and `git -c core.editor=...` are both blocked; use `git commit --no-edit` then a bare `git rebase --continue`
- [MD060 picks the closest-match style](project_md060-closest-match-style.md) — `any` scores each table separately so a narrow one reads as `aligned`, and `--fix` never repairs aligned
- [Pin a spec's empirical premise with a live test](project_pin-a-specs-empirical-premise-with-a-live-test.md) — an "observed layout" claim is falsifiable in one `ls`; walk the real tree, since a fixture only restates the wrong belief
- [read-only-utility ALLOW hides read carve-outs](project_permission-gate-read-only-utility-allow-hides-carve-outs.md) — `cat` already allows any contained/carved-out path, so probe a new READ carve-out with `less` or the Read tool
- [flagScan valueFlags do NOT pick the contained tokens](project_flagscan-value-flag-swallows-path-operand.md) — CORRECTION of the inverted claim: pathOperands ignores the flag model, so mismodelling denies, never fails open
- [Verify a predicted verdict before implementing it](project_verify-a-predicted-verdict-before-implementing-it.md) — acceptance bullets mix the rule (authoritative) with predictions of existing behavior (often wrong); measure each row
- [Audit a prose sweep by added words](project_audit-a-prose-sweep-by-added-words.md) — artifacts hide only in hunks where the sweep ADDED words; re-read comment blocks JOINED, since grep misses a wrap-split phrase
- [No source-lint meta-tests](feedback_no-source-lint-meta-tests.md) — never `go test` the package's own comments/prose; removing one means deleting its README/comment/PR-body claims too
- [Probe the parser AST, not the grammar](project_probe-the-parser-ast-not-the-grammar.md) — mvdan/sh parks Redirs where the grammar does not suggest; measure with a throwaway in-package test, not by reasoning
- [Make the reach structural when enumeration stalls](project_make-the-reach-structural-when-enumeration-stalls.md) — round N+1 finding one more missed position means the hand-listed call sites are the defect; switch to a traversal
- [Rebase absorbs an identical version bump](project_rebase-absorbs-an-identical-version-bump.md) — both sides bumping to the SAME version resolves silently and ships main's value; check `git diff --stat origin/main HEAD`
- [Union-resolve only what the incoming commit changed](project_union-resolution-needs-the-incoming-commits-own-delta.md) — pasting both sides of a MEMORY.md conflict silently reverts main's in-place revisions; read `git show <sha> -- <file>` first
- [Shared-predicate list is one claim](project_shared-predicate-list-is-one-claim.md) — "A, B and C all have property P" needs P re-checked on every member when a finding names one
- [Negative-control the approved snippet](feedback_negative-control-the-approved-snippet.md) — run the finding's doctored cases against the approved code AS WRITTEN, and negate your own fix at its call site
- [Implement the finding's broader rule](feedback_implement-the-findings-broader-rule.md) — when a finding is stricter than the real tool and the breadth is safe-side, ship it and document the divergence
- [Wrong-GOARCH container case is emulated](project_wrong-arch-container-case-is-silently-emulated.md) — podman binfmt_misc runs an amd64 ELF on aarch64 fine; swap the GOOS (Mach-O vs ELF) for a truly unrunnable binary
- [A recorded digest is of a pipeline](project_a-recorded-digest-is-of-a-pipeline.md) — a sha256 quoted in a review is of the author's shell pipeline (`jq -r` adds a newline), not of the value
- [buildvcs stamps the primary clone's HEAD](project_buildvcs-stamp-is-primary-clone-head.md) — a worktree `.git` is a file so Go walks past it; `vcs.revision` is never branch provenance
- [A finding's world-state is a snapshot](project_a-findings-world-state-is-a-snapshot.md) — a finding about external state (unmerged PR, undeployed rule) may already be satisfied when you run; re-read both ends
- [A new arm falsifies the formula back-references](project_new-arm-falsifies-the-formula-back-references.md) — grep the formula symbol (`C ∩ B`), not the arm's keywords; a checked-in consumer list fires for every shared rule
- [Worktree git gate counts git-prefixed basenames](project_worktree-git-gate-counts-git-prefixed-basenames.md) — `git ... plugins/git-tools` is refused as "names git more than once"; a deeper component or the parent dir passes
- [A stand-in arm invalidates the sibling list](project_stand-in-arm-invalidates-the-sibling-lists-definition.md) — define "input minus X" lists against the RESOLVED value, not the input; transcribe the spec's own Output examples
- [A new hedge obsoletes the old absolute](project_new-hedge-obsoletes-the-old-absolute.md) — hedging a fact ("may" where it said "must") contradicts the file's untouched absolutes; grep the fact's modals
- [Drive every path a summary claims](project_drive-every-path-a-summary-claims.md) — "several paths produce this" is an enumeration to RUN, not just reword; one of them was dead code
- [Declaration-vs-state sweep is two-sided](project_declaration-vs-state-sweep-is-two-sided.md) — grade each phrase hit by reading the gate it describes: reword to declaration, to explicit state, or leave alone
- [awk -v eats backslash escapes](project_awk-v-eats-backslash-escapes.md) — `-v x='...\t...'` arrives as a REAL tab, corrupting a literal line; keep a doubled copy for awk, un-doubled for the assertion
- [Missing key ≠ explicit empty](project_missing-key-and-explicit-empty-differ.md) — in yq `@tsv` an unguarded `.key` renders a missing key as literal `null` while `key: ""` gives a truly empty field
- [Old-code claim hits a different guard](project_old-code-claim-hits-a-different-guard.md) — "the old read then did Z" is a prediction; the misparsed value usually trips an UNRELATED guard first
- [Slice the caller to prove a guard is wired](project_slice-the-caller-to-prove-a-guard-is-wired.md) — calling a new abort-guard directly proves only that it returns non-zero; slice the launcher's own gate block
- [`entry #N` counts the merged doc](project_entry-index-is-against-the-structure-the-gate-walks.md) — a per-entry diagnostic numbers against the structure the validator walks, not one tier's own config
- [Probe every tool your prose names](project_probe-every-tool-your-prose-names.md) — a finding's evidence covers only the tools the reviewer ran; probe every tool your own sentence names
- [Pick the fix that spares the costly proof](project_pick-the-fix-that-spares-the-expensive-verification.md) — prefer the remedy leaving the already-real-boot-verified path byte-identical; branch on the RESOLVED share
- [Run the guard on the oldest reachable shell](project_run-the-guard-on-the-oldest-reachable-interpreter.md) — an early guard fails OPEN on bash 3.2 while the file's later `local -A` fails loudly; grade FAILs by label, not count
- [The class is the set of USES, not values](project_the-class-is-the-set-of-uses-not-values.md) — one bad value of a validated field means grep the variable and grade each use position's grammar
- [A finding's exclusivity premise is measurable](project_reviewer-exclusivity-premise-is-measurable.md) — "nothing else reaches $TMPDIR" was false (the gvproxy socket does, every launch); grep what the premise quantifies over
- [Upstream guard picks your test rows](project_upstream-guard-decides-which-rows-can-reach-yours.md) — a row for a narrowed guard must survive the guard BEFORE it; only `ghShieldingFlags` values may be dynamic
- [Help output is not the accepted grammar](project_help-output-is-not-the-accepted-grammar.md) — a finding naming ONE unrendered spelling is about the parser library; its sibling in the same function was the real hole
- [Enumerate completely: derive from the structure](feedback_enumerate-completely-derive-from-the-structure.md) — never append the one flag a finding names; dump the real table, diff EVERY surface, run the checker pre-edit as control
- [Baseline rebuild before a .go comment edit](project_baseline-rebuild-before-editing-a-go-comment.md) — baseline the UNMODIFIED source first, but grade it by nm + build-ID content-hash; a byte cmp expires when the primary clone's HEAD moves
- [Lint sweeps must cover agent-memory](project_lint-sweep-must-cover-the-agent-memory-tree.md) — it is a linted tree with a nested config; sweep the whole tree, and re-lint after writing your own memories
- [Delete the named mechanism to grade the prose](project_delete-the-named-mechanism-to-grade-the-prose.md) — a true verdict does not prove the stated reason; strip the construct the sentence names and re-run
- [A parity fix moves verdicts every direction](project_a-parity-fix-moves-verdicts-in-every-direction.md) — replay a table-derived row list through the pre-fix binary and the rebuilt one; a count is a property of the ROW SET
- [A tier premise can be a vendor fact](project_a-tier-premise-can-be-a-vendor-fact.md) — "this form is the safe one" is a claim about the vendor's product (a secret gist is unlisted, not private)
- [Your change can void a control's liveness anchor](project_your-change-can-void-a-controls-liveness-anchor.md) — a control that pins one still-allowing row goes vacuous when you move that row; re-anchor beyond the diff's reach
- [Word an escalation as the harm, not a blind spot](feedback_word-an-escalation-as-the-harm-not-a-blind-spot.md) — never "the gate cannot tell what this does"; ASK rather than deny, because the gate governs human sessions too
- [Equivalence by sourcing, identity by text](project_equivalence-by-sourcing-identity-by-text.md) — two renderers of one value are compared by SOURCING both (bash `%q` and `shlex.quote` spell it differently); "nothing else changed" is compared by TEXT against the `git show HEAD:`-extracted pre-change function
- [One mechanism, several routes](project_one-mechanism-many-routes-grade-each-member.md) — the named prune is one of three; build the (member × spelling × LAYER) matrix through the real merge, and negative-control with the pre-fix LAUNCHER via `git archive <sha>`
- [Recover deleted memory via git show](project_recovering-deleted-memory-into-docs.md) — the branch has the directory gone; and the recovered doc falsifies CLAUDE.md's "no other file describes X" claims
- [A dangling pointer usually has the rule inline](project_a-dangling-pointer-usually-has-the-rule-inline.md) — repair by deleting the pointer or aiming it at an existing skill, never by duplicating instructions
- [De-specify rather than widen the exception](project_de-specify-rather-than-widen-the-exception.md) — dropping the restated value ends the sweep obligation; then grep the literal and expect only its owner back
- [An owner scope cut leaves advocacy prose behind](project_an-owner-scope-cut-leaves-its-advocacy-prose-behind.md) — deleting the entry is the small part; convert the comment, doc, tests, PR title/body and agent-memory into a documented exclusion
- [A pointer's target must carry the claim](project_pointer-target-must-carry-the-claim.md) — "(see X)" asserts X states the rule; grep the heading and retarget cross-skill when it does not
- [The absolute half of a standing claim](project_the-absolute-half-of-a-standing-claim.md) — "substance stands, phrasing does not": swap the over-broad verb, name the counterexample, say why it is not one
- [Pick the remedy that keeps the exemplar](project_pick-the-remedy-that-keeps-the-exemplar.md) — when the rule CITES the violating site as its model of compliance, move the site, not the rule
- [A merged table row hides two rules](project_a-merged-table-row-hides-two-rules.md) — "either agent's path" collapses two actors whose outcomes differ; split per actor and re-grade the sentence under the table
