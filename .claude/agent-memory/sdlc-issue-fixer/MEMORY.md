# Memory Index

- [vfkit REST shutdown & 5s force-timer](project_vfkit-rest-shutdown-and-5s-force-timer.md) — vfkit force-kills at a hardcoded 5s on any terminating signal; the REST+set-m workaround was tried and verified WRONG (SIGTTIN freezes guest input) — the guest must power itself off instead
- [Backgrounded proc in $() hangs](project_backgrounded-proc-in-command-substitution-hangs.md) — PID="$(start_listener)" hangs unless the bg process's stdout is redirected; it holds the inherited stdout open
- [Docs locality: packaging vs. hook-event](project_docs-locality-packaging-vs-hook-event.md) — plugin-authoring-constraints.md (packaging) vs. hook-event-notes.md (per-event runtime facts); don't conflate
- [Commit heredoc gate](feedback_commit-heredoc-gate.md) — `git commit -m "$(cat <<EOF...)"` is blocked here; use `git commit -F <scratch-file>` for multi-line messages
- [Worktree path, not main clone](feedback_worktree-path-not-main-clone.md) — Read/Edit the worktree-absolute path; main-clone path is a different checkout and gives a stale file view
- [Cross-plugin lib sharing: how much of repo-config does a skill need](project_cross-plugin-lib-sharing-resolved-for-real-config-needs.md) — bare cross-plugin Read of skills/lib/repo-config.md never resolves; ladder = vacuous guard (nothing) → 1-2 real fields (inline parse) → full contract (duplicate or lib-as-skill)
- [Backtick comments in unquoted heredocs](project_backtick-comments-in-unquoted-heredocs.md) — paired backticks in a heredoc comment ARE command substitution; can masquerade as multiple unrelated bugs (claude-vm #105/#161)
- [Real-build verification, not unit tests](project_real-build-verification-not-unit-tests.md) — stub only the blocking external cmd (e.g. podman), run the real script's real path, inspect generated artifacts literally
- [Real boot that exercises mounts, from a worktree](project_real-boot-that-exercises-mounts-from-a-worktree.md) — host-acceptance never writes a mounts.tsv; copy its criterion (b), probe from the stub claude, boot with NO net (worktree path breaks gvproxy's socket), and bump LAUNCHER_LOGIC_REV first
- [PR branch rebased under checkout](project_pr-branch-rebased-under-checkout.md) — this repo's rebase-sweep automation can force-rebase a PR branch mid-session; recover via format-patch+am onto the new tip, never force-push
- [Another worktree holds the branch; cwd resets](project_stale-worktree-holds-branch-and-cwd-does-not-persist.md) — don't inspect or remove it (every cross-worktree git form is gated now); work detached from `origin/<branch>` and push `HEAD:refs/heads/<branch>`
- [Git command-form gate: cd then bare git](feedback_git-command-form-gate-cd-then-bare-git.md) — both `cd X && git ...` and `git -C X ...` are blocked; only a bare `cd X` call then a separate bare `git ...` call works
- [mkosi Minimize=guess sizes fs, not partition](project_mkosi-minimize-guess-sizes-fs-not-partition.md) — Minimize=guess tight-fits the ext4 FS; SizeMinBytes only pads the GPT slot; drop Minimize so the fs fills the partition (verify via GPT+ext4 superblock parse)
- [mkosi v26 source verification technique](project_mkosi-v26-source-verification-technique.md) — gh api contents route (not raw WebFetch, not bare curl) to fetch mkosi's actual Python source; confirms repart/apt-sources default behavior claude-vm fixes depend on
- [apt-get clean verified behavior](project_apt-get-clean-verified-behavior.md) — real-container-verified: removes archives + pkgcache.bin/srcpkgcache.bin, never touches /var/lib/apt/lists
- [claude-vm mkosi installs from outside image](project_claude-vm-mkosi-installs-from-outside-image.md) — a boot-time apt-get needs `apt` explicitly baked into the base Packages= list; mkosi's own build-time apt never leaks into the guest
- [claude-vm inspect raw image with debugfs](project_claude-vm-inspect-raw-image-with-debugfs.md) — dd-carve the ext4 partition then debugfs (no -o offset flag) to verify package contents of a built guest.raw without loop devices
- [apt keyring extension vs content](project_apt-keyring-extension-vs-content.md) — apt >= 2.x picks armored-vs-binary by FILE EXTENSION not content; render_apt_source(_boot) must sniff+rename, pinned signed-by= is exempt; render happens in mkosi's throwaway sandbox tree, not the final rootfs
- [Repo-boundary gate blocks any out-of-repo tool arg](project_repo-boundary-gate-blocks-any-tool-arg-outside-repo.md) — find/cp/awk args resolving outside the repo are blocked even as a cp source; set TMPDIR into the repo before the run that creates scratch files, don't retrofit
- [No invented policy in agent defs](feedback_no-invented-policy-in-agent-defs.md) — never assert a rule in an agent .md unless grepped and confirmed in an actual rules file; a fixer's own prior addition can be the next round's defect
- [Sweep sibling agent guards](project_sweep-sibling-agent-guards.md) — swept guard across 4 sdlc siblings; round 4 found the sweep itself dropped the source's carve-out clause — sweep exceptions, not just the headline rule; sweep inline operational text, collapse shared rationale to a pointer; sweep inside your own hunks, only name out-of-hunk instances
- [Scope guarantee claims to actual caller](feedback_scope-guarantee-claims-to-actual-caller.md) — "autonomous mode always runs fresh" was true only of one caller's worktree habits, not the skill's own logic; name the caller, don't generalize
- [Bounded poll + unbounded wait is unbounded](project_bounded-poll-then-unbounded-wait.md) — the give-up branch must return BEFORE any blocking wait; assert it structurally and write a negative control against the old shape
- [Test harness holds the pipe open](project_test-harness-holds-pipe-open.md) — a long-lived stub in $(...) makes a timing test measure the pipe, not the code; redirect stub stdio and return results via a file
- [Staleness check: both ends, one source](project_staleness-check-both-ends-same-source.md) — a "did it move?" check must record AND re-read from the same authoritative surface (gh headRefOid, not the cached origin/ ref); fix the record site even when the finding names only the re-read
- [Count tallies rot after the list too](project_count-tally-class-includes-back-references.md) — a §7 sweep also covers post-list back-references, YAML `description:` frontmatter, and non-`.md` comments; grade hits against the pr-reviewer's adjudicated boundary
- [Getty respawn is Restart=, not the leading `-`](project_getty-respawn-is-restart-not-dash.md) — stock serial-getty ships Restart=always; also, claude-vm's image identity hash never covers launcher source, so LAUNCHER_LOGIC_REV is the only cache invalidator
- [claude marketplace probe recipe](project_claude-marketplace-probe-recipe.md) — CLI rejects file:// and treats a local path as Directory (no git); roll a real clone back to an old SHA to exercise git; `marketplace update` exits 0 while doing nothing
- [Worktree gate blocks compound bash](project_worktree-isolation-gate-blocks-compound-bash.md) — `$VAR`-path + redirect in one Bash call is refused as unverifiable even when in-bounds; build scratch files with Write, then run bare relative commands
- [Prove a config extends-chain is live](project_prove-config-inheritance-chain-live.md) — cleared violations never prove `extends` resolved; flip a value in the PARENT to watch it propagate, and break the path to confirm it fails closed
- [Lint-config control needs its own directory](project_lint-config-control-needs-its-own-directory.md) — markdownlint-cli2's `--config` loses to directory discovery, so a `--config` baseline silently measures the NEW config; also grep the OLD rule ID for hits the rename un-suppresses
- [PR body is a swept surface](feedback_pr-body-is-a-swept-surface.md) — a fix round that changes a mechanism the PR body names must update its What changed / Decisions bullets that same round
- [git status cannot see main staleness](project_git-status-cannot-see-main-staleness.md) — only `git merge-base --is-ancestor origin/main HEAD` says whether you are behind main, and a CONFLICTING PR never self-heals (the sweep drops DIRTY); after a forced rebase prove it disturbed nothing with a changed-path set check, push with an explicit-SHA lease, and re-check any SHA already quoted in the PR body
- [rebase --continue editor gate](feedback_rebase-continue-editor-gate.md) — `GIT_EDITOR=true git ...` and `git -c core.editor=...` are both blocked; use `git commit --no-edit` then a bare `git rebase --continue`, and strip the baked-in `# Conflicts:` blocks over the whole range via `--exec <script.sh>` (the inline `--exec "git commit --amend …"` is classifier-blocked)
- [MD060 picks the closest-match style](project_md060-closest-match-style.md) — `any` scores each table separately, so a narrow one reads as `aligned`; `--fix` never repairs aligned, so fix the delimiter row toward `compact`
- [Pin a spec's empirical premise with a live test](project_pin-a-specs-empirical-premise-with-a-live-test.md) — an "observed layout" claim in a spec is falsifiable in one `ls`; walk the real tree in a self-skipping test, since a fixture only restates the wrong belief
- [read-only-utility ALLOW hides read carve-outs](project_permission-gate-read-only-utility-allow-hides-carve-outs.md) — `cat` allows any contained/carved-out path already, so probe a new READ carve-out with `less` or the Read tool, never `cat`
- [flagScan valueFlags do NOT pick the contained tokens](project_flagscan-value-flag-swallows-path-operand.md) — CORRECTION of the inverted claim: pathOperands ignores the flag model, so mismodelling makes the verdict stricter (deny), never fail-open — binary-verified
- [Verify a predicted verdict before implementing it](project_verify-a-predicted-verdict-before-implementing-it.md) — an issue's acceptance bullets mix the rule (authoritative) with predictions of existing behavior (often wrong); measure each row, implement uniformly, report divergences
- [Worktree agent refuses compound / $VAR commands](feedback_worktree-agent-refuses-compound-and-var-args.md) — `mkdir && go build` and `bash s.sh bin "$PWD"` are both "too complex to verify"; one command per call, literal paths, no `$PWD`
- [Audit a prose sweep by added words](project_audit-a-prose-sweep-by-added-words.md) — artifacts hide only in hunks where the sweep ADDED words; filter to those, and re-read comment blocks JOINED — a line-based grep cannot see a wrap-split phrase
- [No source-lint meta-tests](feedback_no-source-lint-meta-tests.md) — never `go test` the package's own comments/prose (nor substitute a build tag or linter config); removing one means deleting its README/comment/PR-body claims too
- [Probe the parser AST, not the grammar](project_probe-the-parser-ast-not-the-grammar.md) — mvdan/sh: a bare `> f` has Cmd==nil, and `a && b > f` / `f() { …; } > f` park Redirs on the INNER stmt; measure with a throwaway in-package test, and for "which positions" questions probe real bash AND the parser — they disagree both ways
- [Make the reach structural when enumeration stalls](project_make-the-reach-structural-when-enumeration-stalls.md) — round N+1 finding one more missed position means the hand-listed call sites are the defect; switch to a traversal and pin it with a graded-count == parsed-node-count equality- [Rebase absorbs an identical version bump](project_rebase-absorbs-an-identical-version-bump.md) — both sides bumping a plugin to the SAME version resolves silently and leaves the branch shipping main's value; can fire twice in one round, so check `git diff --stat origin/main HEAD` for the missing plugin.json after every rebase (differing values conflict loudly — take the greater, don't re-bump)
- [Union-resolve only what the incoming commit changed](project_union-resolution-needs-the-incoming-commits-own-delta.md) — pasting both sides of a MEMORY.md conflict silently reverts main's in-place revisions; read `git show <sha> -- <file>` to see which lines that commit meant to touch, and merge (never pick) the lines both sides revised
- [Shared-predicate list is one claim](project_shared-predicate-list-is-one-claim.md) — "A, B and C all have property P" needs P re-checked on every member when a finding names one; a phrase-scoped list also claims to be exhaustive — grep the phrase and name the near-miss exclusion
- [Negative-control the approved snippet](feedback_negative-control-the-approved-snippet.md) — run the finding's doctored cases against the approved code AS WRITTEN (#217 left 137/139 failing open); flip the code to prove a counterfactual you write in a comment, and negate your own fix by passing `nil` at its one call site
- [Implement the finding's broader rule](feedback_implement-the-findings-broader-rule.md) — when a finding is stricter than the real tool (`-ht` is pflag's hostname=t, not `--show-token`) and the breadth is safe-side, ship the finding's rule and document the divergence in the arm
- [Wrong-GOARCH container case is emulated](project_wrong-arch-container-case-is-silently-emulated.md) — podman binfmt_misc runs an amd64 ELF on aarch64 fine; swap the GOOS (Mach-O vs ELF) for a genuinely unrunnable binary, and print raw exit statuses
- [A recorded digest is of a pipeline](project_a-recorded-digest-is-of-a-pipeline.md) — a sha256 quoted in a review is of the author's shell pipeline (`jq -r` adds a trailing newline), not of the value; match it on the PRE-change ref first, and prefer comparing git blob hashes
- [buildvcs stamps the primary clone's HEAD](project_buildvcs-stamp-is-primary-clone-head.md) — a worktree `.git` is a file so Go walks past it; `vcs.revision` is never branch provenance — prove currency with `go tool nm` identity, a `-trimpath` rebuild+shasum, or an event replayed through the `git show HEAD:<bin>` pre-fix binary
- [A finding's world-state is a snapshot](project_a-findings-world-state-is-a-snapshot.md) — a finding about external state (unmerged companion PR, undeployed rule) may already be satisfied when you run; re-read both ends, then flip the remedy's tense
- [A new arm falsifies the formula back-references](project_new-arm-falsifies-the-formula-back-references.md) — grep the formula symbol (`C ∩ B`), not the arm's keywords; a checked-in consumer list fires for every rule those files share
- [Worktree git gate counts git-prefixed basenames](project_worktree-git-gate-counts-git-prefixed-basenames.md) — `git … plugins/git-tools` is refused as "names git more than once"; a deeper component or the parent dir passes
- [A stand-in arm invalidates the sibling list](project_stand-in-arm-invalidates-the-sibling-lists-definition.md) — define "input minus X" lists against the RESOLVED value, not the input; transcribe the spec's own Output examples to catch the mismatch
- [A new hedge obsoletes the old absolute](project_new-hedge-obsoletes-the-old-absolute.md) — hedging a fact ("may" where it said "must") contradicts the file's own untouched absolute phrasings; grep the fact's modals, not your hunk
- [Drive every path a summary claims](project_drive-every-path-a-summary-claims.md) — "several paths produce this" is an enumeration to RUN, not just reword; one was dead code because a tab-IFS `read` swallows an empty field (the rule itself is in the root CLAUDE.md)
- [Declaration-vs-state sweep is two-sided](project_declaration-vs-state-sweep-is-two-sided.md) — grade each phrase hit by reading the gate it describes: reword-to-declaration, reword-to-explicit-state, or leave alone (a sibling gate with no membership test isn't in the class)
- [awk -v eats backslash escapes](project_awk-v-eats-backslash-escapes.md) — `-v x='…\t…'` arrives as a REAL tab, silently corrupting a literal line you reconstruct; keep a doubled copy for awk and an un-doubled one for the assertion (`\x27` is GNU-only too)
- [Missing key ≠ explicit empty](project_missing-key-and-explicit-empty-differ.md) — probe BOTH before calling an empty field impossible; in yq `@tsv` an unguarded `.key` renders a missing key as literal `null` while `key: ""` gives a truly empty field
- [Old-code claim hits a different guard](project_old-code-claim-hits-a-different-guard.md) — "the old read then did Z" is a prediction, and the misparsed value usually trips an UNRELATED downstream guard first; run the extracted pre-change function on the exact record
- [Slice the caller to prove a guard is wired](project_slice-the-caller-to-prove-a-guard-is-wired.md) — calling a new abort-guard directly proves only that it returns non-zero; slice the launcher's own gate block by line range, and always include the passing cases
- [`entry #N` counts the merged doc](project_entry-index-is-against-the-structure-the-gate-walks.md) — a per-entry diagnostic numbers against the structure the validator walks, so "its position in this list" in one tier's example config is false; measure with a two-tier fixture through the real merge + real gate
- [Probe every tool your prose names](project_probe-every-tool-your-prose-names.md) — a finding's evidence covers only the tools the reviewer ran (`mv`); if your doc sentence names `git config` and `sed -i`, probe those too, plus the contrasting case, and quote the real error strings
- [Pick the fix that spares the costly proof](project_pick-the-fix-that-spares-the-expensive-verification.md) — among acceptable remedies prefer the one leaving the already-real-boot-verified path byte-identical; branch on the RESOLVED share, not the config knob, and say in the PR body which path is untouched
- [Run the guard on the oldest reachable shell](project_run-the-guard-on-the-oldest-reachable-interpreter.md) — an early guard fails OPEN on bash 3.2 while the file's later `local -A` fails loudly; then grade the old shell's FAILs by diffing labels across `git archive`d trees, never by counts
- [The class is the set of USES, not values](project_the-class-is-the-set-of-uses-not-values.md) — one bad value of a validated field means grep the variable and grade each use position's grammar (path component / argv word / delimited field); guard the fail-OPEN members
- [A finding's exclusivity premise is measurable](project_reviewer-exclusivity-premise-is-measurable.md) — "nothing else reaches $TMPDIR" was false (the gvproxy socket does, every launch); grep the construct the premise quantifies over before repeating it in prose
- [Upstream guard picks your test rows](project_upstream-guard-decides-which-rows-can-reach-yours.md) — a row for a narrowed guard must survive the guard BEFORE it; in the gate only `ghShieldingFlags` values may be dynamic, and `-f`/`-F` shield only with a pinned `key=`
- [Help output is not the accepted grammar](project_help-output-is-not-the-accepted-grammar.md) — a finding naming ONE unrendered spelling is about the parser library; pflag's `-h` was the Low, its `-F=path` sibling in the same function was an allow-the-exfil hole
- [Enumerate completely: derive from the structure](feedback_enumerate-completely-derive-from-the-structure.md) — never append the one flag a finding names; dump the real table, diff EVERY surface (PR body included), run the checker pre-edit as its control
- [Baseline rebuild before a .go comment edit](project_baseline-rebuild-before-editing-a-go-comment.md) — cmp a rebuild of the UNMODIFIED source against the committed binaries first, so the post-edit `go tool nm` identity is attributable to your edit
- [Lint sweeps must cover agent-memory](project_lint-sweep-must-cover-the-agent-memory-tree.md) — it is a linted tree with a nested config; sweep the PR's `*.md` diff AND the whole tree, re-lint after writing your own memories, and prove the `extends` is live with an MD055 probe
- [Delete the named mechanism to grade the prose](project_delete-the-named-mechanism-to-grade-the-prose.md) — a true verdict does not prove the stated reason; strip the construct the sentence names out of its own example and re-run, with a positive control row
- [Probe a mutating verb without mutating it](project_probe-a-mutating-verb-without-mutating-it.md) — make the FLAG PARSE fail (invalid typed value); a cluster can eat the operand you counted on to abort, and `gh gist create -pd x` then publishes STDIN
- [A parity fix moves verdicts every direction](project_a-parity-fix-moves-verdicts-in-every-direction.md) — replay one row list through the pre-fix committed binary and the rebuilt one side by side; report the permissive moves loudest
